# {{PROJECT_NAME}} · Evidence Ledger

只记录实际发生的证据，不沿用上一轮项目的 PASS。

| 时间 | 阶段 | 动作/命令 | 结果 | 证据路径 | 状态边界 |
|---|---|---|---|---|---|
| {{CREATED_AT}} | scaffold | 创建项目结构 | 完成 | 当前项目目录 | 仅证明结构已创建，不证明 PRD、生成或交付完成 |

## 状态词

- `draft`：正在整理，不能进入下一门禁；
- `ready`：内容已具备评审条件；
- `approved`：人工已确认；
- `validated`：约定的机械检查已通过；
- `pending`：外部检查或信息仍缺失；
- `blocked`：缺失项会实质改变结果，当前不能继续。
